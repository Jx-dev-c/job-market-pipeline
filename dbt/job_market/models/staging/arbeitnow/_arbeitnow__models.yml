version: 2

models:
  - name: stg_arbeitnow__jobs
    columns:
      - name: job_key
        # não é unique em staging (uma linha por dia por vaga); ver stg_adzuna__jobs
        tests:
          - not_null
      - name: external_id
        tests:
          - not_null
      - name: title
        tests:
          - not_null
      - name: url
        tests:
          - not_null
